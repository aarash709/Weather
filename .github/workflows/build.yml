name: build

on:
  push:
    branches: master
  pull_request:
  concurrency:
    group: build-${{ github.ref }}
    cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: Validate Gradle Wrapper
        uses: gradle/wrapper-validation-action@v1

      - name: Setup JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: zulu
          java-version: 17

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      - name: Grant gradle executable permission
        run: chmod +x gradlew

      - name: Run Local Unit Tests
        run: ./gradlew testdebug

#  androidTest:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: checkout
#        uses: actions/checkout@v3
#
#      - name: Validate Gradle Wrapper
#        uses: gradle/wrapper-validation-action@v1
#
#      - name: Setup JDK 17
#        uses: actions/setup-java@v3
#        with:
#          distribution: zulu
#          java-version: 17
#
#      - name: Setup Gradle
#        uses: gradle/gradle-build-action@v2
#
#      - name: Grant gradle executable permission
#        run: chmod +x gradlew
#       use an android test runner like "Android Emulator Runner action"
#      - name: Run Instrumentation tests
#        run: ./gradlew cAT