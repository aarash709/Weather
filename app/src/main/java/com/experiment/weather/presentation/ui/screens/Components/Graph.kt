package com.experiment.weather.presentation.ui.screens.Components

import androidx.compose.runtime.Composable
import androidx.compose.ui.tooling.preview.Preview

@Composable
@Preview(showBackground = true)
fun Preview() {
//    Canvas(modifier = Modifier.fillMaxSize()) {
//        val canvasSize = size
//        val canvaswidth = canvasSize.width
//        val canvasHeight = canvasSize.height
//        val x = canvaswidth / 5f
//        val y = canvasHeight / 5f
//
//        val points = mutableListOf<Offset>(
//            Offset(x, y), Offset(x + 2, y + 1),
//            Offset(x + 4, y + 3)
//        )
//
//
//        drawPoints(
//            points,
//            PointMode.Lines,
//            color = Color.Red,
//            strokeWidth = 20f
//        )
//
////        drawCircle(
////            Color.Red,
////            radius = size.minDimension/4,
////            center = Offset(x = canvasSize.width / 2,y = canvasSize.height /2)
////        )
//    }
}

/*Canvas(modifier = Modifier.fillMaxSize()){
            val canvasSize = size
            val canvaswidth= canvasSize.width
            val canvasHeight= canvasSize.height
            val x = canvaswidth / 5f
            val y = canvasHeight/ 5f

            val points = mutableListOf<Offset>(Offset(x,y),Offset(x+100,y+150),
                Offset(x+100,y+180))

            drawPoints(
                points,
                PointMode.Lines,
                color = Color.Red,
                strokeWidth = 20f
            )

            drawCircle(
                Color.Red,
                radius = size.minDimension/4,
                center = Offset(x = canvasSize.width / 2,y = canvasSize.height /2)
            )

        }*/