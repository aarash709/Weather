package com.experiment.weather.common.buildsrc

object Libs {
    object Compose {
        private const val version = "1.0.4"
        const val composeUi = "androidx.compose.ui:ui:$version"
        const val composeMaterial = "androidx.compose.material:material:$version"
        const val composeTooling = "androidx.compose.ui:ui-tooling-preview:$version"
        const val composeRuntime = "androidx.compose.runtime:runtime:$version"
        const val composeLivedata = "androidx.compose.runtime:runtime-livedata:$version"
        const val composeFoundatoin = "androidx.compose.foundation:foundation:$version"
        const val composeAnimation = "androidx.compose.animation:animation:$version"
        const val composeCompiler = "androidx.compose.compiler:compiler:$version"
    }
}